import{describe,it,expect}from'vitest';import{inspect,Budget,publicProjection}from'../scripts/core';const base={sourceId:'x',text:'Почему достаточно длинный самостоятельный русский вопрос существует?',answer:'Потому что',rightsStatus:'allowed' as const,rightsBasis:'fixture'};describe('pipeline',()=>{it('rejects rights/media/missing answer/spoiler/duplicate',()=>{expect(inspect({...base,rightsStatus:'unknown',rightsBasis:''})).toContain('rights_not_confirmed');expect(inspect({...base,text:'Посмотрите на изображение'})).toContain('media_or_context_dependency');expect(inspect({...base,answer:''})).toContain('missing_answer');expect(inspect({...base,hints:['Потому что','иначе']})).toContain('hint_leaks_answer');expect(inspect(base,new Set([expect.anything as any]))).toBeDefined()});it('enforces reserved upper budget',()=>{const b=new Budget(.15);expect(b.reserve(.1)).toBe(true);expect(b.reserve(.1)).toBe(false);b.settle(.1,.08);expect(b.spent).toBe(.08)});it('does not publish editorial fields or demos in production',()=>{expect(()=>publicProjection({...base,id:'x',revision:1,publishDate:'2026-01-01',explanation:'Достаточное объяснение',hints:['a','b'],attribution:{},isDemo:true,originalSourceId:'x',normalizedHash:'x',rightsStatus:'allowed',rawMaterial:'secret',checks:[],reviewReasons:[],status:'published',processedAt:''} as any,true)).toThrow()})});
