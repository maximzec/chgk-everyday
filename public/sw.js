const SHELL='vecher-shell-v1',CONTENT='vecher-content-v1';const shell=['/','/archive/','/app.js','/offline.html','/site.webmanifest'];self.addEventListener('install',e=>e.waitUntil(caches.open(SHELL).then(c=>c.addAll(shell))));self.addEventListener('activate',e=>e.waitUntil(self.clients.claim()));self.addEventListener('message',e=>{if(e.data==='activate')self.skipWaiting()});self.addEventListener('fetch',e=>{const u=new URL(e.request.url);if(u.origin!==location.origin)return;if(u.pathname==='/sw.js'||u.pathname.endsWith('manifest.json'))return;e.respondWith((async()=>{const cached=await caches.match(e.request);if(cached)return cached;try{const r=await fetch(e.request);if(r.ok&&(e.request.mode==='navigate'||u.pathname.startsWith('/content/'))){const c=await caches.open(u.pathname.startsWith('/content/')?CONTENT:SHELL);c.put(e.request,r.clone())}return r}catch{if(e.request.mode==='navigate')return await caches.match('/offline.html');throw Error('offline')}})())});
